<%= form_with model: post, url: posts_path, local: false, id: "new_post_form" do |f| %>
  <div>
    <%= f.label :content, "Write a new post:" %><br>
    <%= f.text_area :content, rows: 3 %>
  </div>
  <div>
    <%= f.submit "Post" %>
  </div>
<% end %>
